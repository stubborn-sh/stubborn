apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-cloud-contract-broker.fullname" . }}
  labels:
    {{- include "spring-cloud-contract-broker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spring-cloud-contract-broker.selectorLabels" . | nindent 4 }}
